
  # Nuevo prototipo desde cero

  This is a code bundle for Nuevo prototipo desde cero. The original project is available at https://www.figma.com/design/Qv7abPnCLa5MNQMyIMxD72/Nuevo-prototipo-desde-cero.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  